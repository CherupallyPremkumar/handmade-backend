<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.homebase.admin.mapper.NotificationMapper">

    <resultMap id="notificationResultMap" type="com.homebase.admin.entity.Notification">
        <id property="id" column="id"/>
        <result property="customerId" column="customer_id"/>
        <result property="title" column="title"/>
        <result property="message" column="message"/>
        <result property="type" column="type"/>
        <result property="isRead" column="is_read"/>
        <result property="tenantId" column="tenant_id"/>
    </resultMap>

    <select id="findByCustomerIdAndTenantId" resultMap="notificationResultMap">
        SELECT * FROM notifications WHERE customer_id = #{customerId} AND tenant_id = #{tenantId} AND deleted = false ORDER BY created_at DESC
    </select>

    <select id="findUnreadByCustomerIdAndTenantId" resultMap="notificationResultMap">
        SELECT * FROM notifications WHERE customer_id = #{customerId} AND is_read = false AND tenant_id = #{tenantId} AND deleted = false ORDER BY created_at DESC
    </select>

    <select id="findByIdAndTenantId" resultMap="notificationResultMap">
        SELECT * FROM notifications WHERE id = #{id} AND tenant_id = #{tenantId} AND deleted = false
    </select>

    <select id="countUnreadByCustomerId" resultType="long">
        SELECT COUNT(*) FROM notifications WHERE customer_id = #{customerId} AND is_read = false AND tenant_id = #{tenantId} AND deleted = false
    </select>

    <select id="findRecentByCustomerId" resultMap="notificationResultMap">
        SELECT * FROM notifications WHERE customer_id = #{customerId} AND tenant_id = #{tenantId} AND deleted = false ORDER BY created_at DESC LIMIT #{limit}
    </select>

</mapper>
